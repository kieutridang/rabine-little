/*
 * Site Detail
 *
 * This contains all the text for the SiteDetail
 */

import { defineMessages } from 'react-intl';

export default defineMessages({
  validationNote_Editor: {
    id: 'boilerplate.containers.SiteDetail.validation.note_editor.required',
    defaultMessage: 'Note is required',
  },
});
