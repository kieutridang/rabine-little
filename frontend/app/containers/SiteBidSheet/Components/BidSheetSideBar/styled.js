import styled from 'styled-components';

const backgroundColor = '#ffffff';

export const BidSheetSideBarWrapper = styled.section`
  grid-area: sidebar;
  position: relative;
  height: 83vh;
  background: ${backgroundColor};
  border: 1px solid #efeff1;
`;
